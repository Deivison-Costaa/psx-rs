# Sem `if:` nos passos do job check, nunca: um passo pulado não mede nada e deixa o job
# verde. O meta-teste ci_workflow.rs (item 0.3) reprova o PR que reintroduzir condicional.
# A versão do compilador vem do rust-toolchain.toml e de nenhum outro lugar — este workflow
# não escolhe canal (toolchain_pin.rs reprova quem voltar a escolher).
name: CI

# Medido no PR #189: o passo de suite gastou 188 s compilando/linkando 145 binarios de teste e
# 14 s rodando — no runner os diagnosticos pesados terminam em 5 ms porque BIOS e disco nao
# existem la. O gargalo e link, e debuginfo completo domina o link. Desligar aqui, e nao no
# Cargo.toml, deixa o repositorio local depuravel.
env:
  CARGO_PROFILE_DEV_DEBUG: "0"
  CARGO_PROFILE_TEST_DEBUG: "0"

on:
  push:
    branches: [main]
  pull_request:

jobs:
  check:
    name: check
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: instala o compilador pinado no rust-toolchain.toml
        run: rustup toolchain install --no-self-update && cargo --version
      - uses: Swatinem/rust-cache@v2
      - run: sudo apt-get update && sudo apt-get install -y libasound2-dev libudev-dev libxkbcommon-dev
      - run: cargo fmt --all -- --check
      - run: cargo clippy --all-targets -- -D warnings
      # cargo test roda os binarios de teste em SERIE; o nextest roda em paralelo entre eles.
      # O ganho e local (56 s contra 449 s): no CI a execucao ja era 14 s e o custo esta no
      # link, tratado pelo `env` do topo. O nextest nao roda doctest, dai o passo separado.
      - name: instala cargo-nextest (binario pronto, sem compilar)
        run: curl -LsSf https://get.nexte.st/latest/linux | tar zxf - -C "${CARGO_HOME:-$HOME/.cargo}/bin"
      - run: cargo nextest run --all-targets --no-fail-fast
      - run: cargo test --all --doc

  scoreboard:
    name: scoreboard
    runs-on: ubuntu-latest
    needs: check
    if: success()
    timeout-minutes: 15
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: instala o compilador pinado
        run: rustup toolchain install --no-self-update && cargo --version
      - uses: Swatinem/rust-cache@v2
      - run: sudo apt-get update && sudo apt-get install -y libasound2-dev libudev-dev libxkbcommon-dev
      - name: baixa EXEs de teste
        run: pwsh scripts/fetch-test-exes.ps1
        continue-on-error: true
      - name: build psx-cli
        run: cargo build --release -p psx-cli
      - name: roda scoreboard
        run: pwsh scripts/scoreboard.ps1
      - name: publica placar na branch scoreboard-data
        if: github.event_name == 'push' && github.ref == 'refs/heads/main'
        run: |
          git checkout scoreboard-data 2>/dev/null || git checkout --orphan scoreboard-data
          git rm -rf --cached . 2>/dev/null || true
          if [ -f scoreboard-data.csv ]; then
            tail -n +2 logs/scoreboard.csv >> scoreboard-data.csv
          else
            cp logs/scoreboard.csv scoreboard-data.csv
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add scoreboard-data.csv
          git diff --staged --quiet || git commit -m "scoreboard: $(date -u +%Y-%m-%dT%H:%M:%SZ)"
           git push origin scoreboard-data

  mutantes:
    name: mutantes
    runs-on: ubuntu-latest
    needs: check
    if: github.event_name == 'pull_request'
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: instala o compilador pinado
        run: rustup toolchain install --no-self-update && cargo --version
      - uses: Swatinem/rust-cache@v2
        with:
          save-if: false
      - run: sudo apt-get update && sudo apt-get install -y libasound2-dev libudev-dev libxkbcommon-dev
      - name: roda bateria de mutacao nos manifestos alterados
        run: pwsh scripts/mutantes.ps1 -Alterados
