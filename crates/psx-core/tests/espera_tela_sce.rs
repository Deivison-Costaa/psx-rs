#[test]
fn sample_pcs_amostra_o_loop_na_janela() {
    eprintln!(
        "psx-cli: teste real em crates/psx-cli/tests/espera_tela_sce.rs — bateria \
         manual (invariante 29). Stub para o portao bateria_nomes_de_teste_existem."
    );
}
