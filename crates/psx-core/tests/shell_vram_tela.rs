#[test]
fn dump_vram_grava_fill_convertido_para_15bpp() {
    eprintln!(
        "psx-cli: teste real em crates/psx-cli/tests/shell_vram_tela.rs — bateria \
         manual (invariante 29). Stub para o portao bateria_nomes_de_teste_existem."
    );
}
